import { Routes, RouterModule } from '@angular/router';
import { NgModule } from '@angular/core';


const elroutes: Routes = [
];

@NgModule({
    imports: [RouterModule.forChild(elroutes)],
    exports: [RouterModule ]
  })
export class FccElRoutes { }
