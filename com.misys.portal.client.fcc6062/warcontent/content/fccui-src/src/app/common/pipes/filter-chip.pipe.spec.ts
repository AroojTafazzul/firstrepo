import { FilterChipPipe } from './filter-chip.pipe';

describe('FilterChipPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChipPipe();
    expect(pipe).toBeTruthy();
  });
});
