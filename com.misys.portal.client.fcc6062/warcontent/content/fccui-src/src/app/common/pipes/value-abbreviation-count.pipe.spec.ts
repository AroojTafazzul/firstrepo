import { ValueAbbreviationCountPipe } from './value-abbreviation-count.pipe';

describe('Pipe: ValueAbbreviationCounte', () => {
  it('create an instance', () => {
    const pipe = new ValueAbbreviationCountPipe();
    expect(pipe).toBeTruthy();
  });
});
