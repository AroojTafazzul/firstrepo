import { FormResolverDirective } from './form-resolver.directive';

describe('FormResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new FormResolverDirective();
    expect(directive).toBeTruthy();
  });
});
