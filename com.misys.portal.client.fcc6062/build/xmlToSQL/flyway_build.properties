# current webapp version.
# IMPORTANT: maintain this property correctly so that correct migration scripts are recognized by Flyway 
webapp.version=5.4.0.0.0

# Flyway directories
flyway.root.dir=../target/flyway

flyway.ddl.prefix=01
flyway.dml.prefix=02

webapp.resources.dir = ${webapp.dir}/resources
# Migration dir
client.migration.dir = ../../xmlToSQL/clients/migration_script